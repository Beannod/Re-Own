// Placeholder for index page interactivity
// Future: animate feature list, handle deep-linking or telemetry.
(function(){
  // Example: smooth scroll to top if hash present
  if (window.location.hash) {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  }
})();